// Ant Design için temel tema token ayarları - Base theme token configuration for Ant Design
import type { ThemeConfig } from "antd";

// Global tema ayarları - Global theme configuration
export const theme: ThemeConfig = {
  token: {
    colorPrimary: "#1677ff",
    borderRadius: 8,
    fontSize: 14,
    controlHeight: 40,
  },
};
